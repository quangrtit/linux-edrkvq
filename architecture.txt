vcs-ajiant-edr/
├── libbpf/               # libbpf
├── bpftool/              # bpftool (dùng để build *.skel.h)
├── vmlinux.h/            # vmlinux.h cho CO-RE cái này sẽ được tạo ra tùy thuộc vào kernel chứa các struct enum hệ thống
├── tools/
│   └── cmake/            # Module hỗ trợ CMake (FindLibBpf, FindBpfObject)
│   |    ├── FindBpfObject.cmake
│   |    ├── FindLibBpf.cmake
│   |── gen_vmlinux_h.sh
├── src/
│   ├── kernel/     # Viết các chương trình bpf ở kernel
│   │ 
│   ├── user/       # Viết các chương trình ở lớp user
│   │   
├── tests/
│   └── test_self_defense.c
│   └── test_ioc_block.c
├── configs/
│   └── configs.json              # chứa policy được định nghĩa sẽ gửi từ user xuống kernel tăng độ linh hoạt cho chính sách bảo vệ
├── test_environment/     # Chứa mã kiểm thử tấn công vào tiến trình, file, các hành vi độc hại cho việc kiểm thử khả năng bảo vệ
├── CMakeLists.txt 
